import { OnboardingForm } from "@/components/private/onboarding-form";

export const metadata = {
  title: "Embarquement | Espace étudiant — PieAgency",
};

export default function StudentOnboardingPage() {
  return <OnboardingForm />;
}
